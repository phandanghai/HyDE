# Bundle: vim

- **Package:** `vim`
- **Mức độ:** 🟢 **Tier A — Độc lập**: copy là chạy, không cần HyDE.

> vimrc của bạn + hyde.vim + colorscheme wallbash.vim (tĩnh).

## Cài đặt
```bash
sudo pacman -S --needed vim
bash apply.sh
```

## File trong bundle
```
home/.config/vim/colors/wallbash.vim
home/.config/vim/hyde.vim
home/.config/vim/vimrc
```

## Giữ nguyên (không ghi đè nếu đã có)
```
.config/vim/vimrc
```
