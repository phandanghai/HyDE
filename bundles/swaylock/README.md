# Bundle: swaylock

- **Package:** `swaylock-effects`
- **Mức độ:** 🟢 **Tier A — Độc lập**: copy là chạy, không cần HyDE.

> Màn khóa swaylock. File config đơn (màu có thể do wallbash chèn).

## Cài đặt
```bash
sudo pacman -S --needed swaylock-effects
bash apply.sh
```

## File trong bundle
```
home/.config/swaylock/config
```

## Giữ nguyên (không ghi đè nếu đã có)
```
```
