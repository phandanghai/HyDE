# Bundle: starship

- **Package:** `starship`
- **Mức độ:** 🟢 **Tier A — Độc lập**: copy là chạy, không cần HyDE.

> Prompt thuần. Bật bằng: thêm 'eval "$(starship init zsh)"' vào shell.

## Cài đặt
```bash
sudo pacman -S --needed starship
bash apply.sh
```

## File trong bundle
```
home/.config/starship/powerline.toml
home/.config/starship/starship.toml
```

## Giữ nguyên (không ghi đè nếu đã có)
```
.config/starship/starship.toml
.config/starship/powerline.toml
```
